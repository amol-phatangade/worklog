[project]
name = "worklog"
version = "0.1.0"
description = "Local-first CLI tool to track daily work and generate summaries"
requires-python = ">=3.10"

dependencies = [
  "typer>=0.9.0",
  "rich",
  "openai"
]

[project.scripts]
worklog = "worklog.cli:main"


